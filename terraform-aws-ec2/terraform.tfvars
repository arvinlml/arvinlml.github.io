# Example Terraform variables - copy and modify as needed
aws_region       = "us-east-1"
instance_type    = "t3.micro"
instance_name    = "my-web-server"
allowed_ssh_cidr = "0.0.0.0/0"  # Change this to your IP for better security
